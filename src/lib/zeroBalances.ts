/**
 * «نمایش حساب‌های با موجودی صفر» — the viewer's choice on «حساب‌ها», kept in a
 * cookie so the server renders the list the chosen way. Shown by default:
 * `"0"` hides accounts whose balance is zero.
 */
export const SHOW_ZERO_BALANCES_COOKIE = "pwos_show_zero_balances";
